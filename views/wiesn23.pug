extends layout

block content
  h1 Wiesn 23
  each image in images
    image
      if image.video
        video.gallery(controls width=256)
          source(src='images/wiesn23/' + image.filename)
        a(href='images/wiesn23/' + image.filename) ⬆️ Save Video
      else
        img.gallery(src='images/wiesn23/' + image.filename width=256)
