extends layout

block content
  h1=title
  each image in images
    div
      if image.video
        video.gallery(controls width=256 poster='images/gallery/' + title + '/thumbnails/' + image.filename + '.jpg')
          source(src='images/gallery/' + title + '/' + image.filename)
        br
        h2: a(href='images/gallery/' + title + '/' + image.filename) ⬆ Video File
      else
        a(href='images/gallery/' + title + '/' + image.filename)
          img.gallery(src='images/gallery/' + title + '/thumbnails/' + image.filename width=256)
    p &nbsp;
